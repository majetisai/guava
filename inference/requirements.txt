# Guava inference service.
# Model libraries are added per feature so the service stays small until needed.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20

# STT (CPU)
faster-whisper==1.1.1
requests==2.34.2

# TTS (CPU) — needs Python <3.13
kokoro>=0.9.4
soundfile>=0.12.1

# Voice cloning (CPU) — Coqui XTTS-v2, zero-shot
coqui-tts==0.27.5
torchaudio
torchcodec
# coqui needs the pre-5.0 transformers API (isin_mps_friendly)
transformers==4.57.6
